# Middleware 概述

> 在每一步控制和自定义 Agent 执行

中间件提供了一种更紧密控制 Agent 内部发生什么的方式。中间件适用于以下场景：

* 使用日志、分析和调试跟踪 Agent 行为。
* 转换提示、工具选择和输出格式。
* 添加重试、回退和早期终止逻辑。
* 应用速率限制、护栏和 PII 检测。

通过将中间件传递给 `create_agent` 来添加：

```python
from langchain.agents import create_agent
from langchain.agents.middleware import SummarizationMiddleware, HumanInTheLoopMiddleware

agent = create_agent(
    model="gpt-5.4",
    tools=[...],
    middleware=[
        SummarizationMiddleware(...),
        HumanInTheLoopMiddleware(...)
    ],
)
```

---

## Agent 循环

核心 Agent 循环涉及调用模型、让它选择要执行的工具，然后在不再调用工具时完成：

```
开始 → 调用模型 → 模型选择工具？─ 是 → 执行工具 → 回到模型
                    │
                    否 → 结束
```

中间件在每个步骤前后暴露钩子：

```
开始
  ↓
before_model 钩子
  ↓
调用模型
  ↓
after_model 钩子
  ↓
模型选择工具？── 是 → before_tool 钩子 → 执行工具 → after_tool 钩子 → 回到 before_model
  │
  否
  ↓
结束
```

---

## 在 LangGraph 工作流中使用中间件

中间件不是独立的运行时：钩子在 `create_agent` 返回的已编译 LangGraph 内部运行。你可以将整个 Agent（包括中间件）作为节点或子图放入更大的 StateGraph 中，每个中间件钩子继续运行。

当周围的拓扑不仅仅是标准的"循环直到完成"时使用这种模式：在路由到多个 Agent 之前分类输入、并行分散工作、或将 Agent 调用与确定性步骤拼接在一起。

`HumanInTheLoopMiddleware` 根据每个工具的 `.name` 进行匹配。在 Python 中，`@tool` 装饰的函数从函数名获取名称。

```python
from langchain.agents import AgentState, create_agent
from langchain.agents.middleware import HumanInTheLoopMiddleware
from langgraph.graph import START, StateGraph

email_agent = create_agent(
    model="claude-sonnet-4-6",
    tools=[read_email, send_email],
    middleware=[HumanInTheLoopMiddleware(interrupt_on={"send_email": True})],
)

graph = (
    StateGraph(AgentState)
    .add_node("classify", classify_node)
    .add_node("email_agent", email_agent)
    .add_edge(START, "classify")
    .add_conditional_edges("classify", route)
    .compile()
)
```

HITL 中断、总结、PII 脱敏、重试和任何自定义钩子都随 Agent 节点一起旅行。

---

## 相关资源

| 资源 | 说明 |
|------|------|
| **内置中间件** | 探索常见用例的内置中间件 |
| **自定义中间件** | 使用钩子和装饰器构建自己的中间件 |
| **中间件 API 参考** | 完整的中间件 API 参考 |
| **中间件集成** | Anthropic、AWS、OpenAI 等的 provider 特定中间件 |
| **测试 Agent** | 使用 LangSmith 测试你的 Agent |
